# rag_system 项目结构说明

> Track D「项目结构规范」落地产物 · 2026-08-03
> 原则：**只补文档，不动文件**（避免踩坑 #9/#10 的跨目录移动问题）

## 一、目录结构

```
rag_system/
├── .venv/              虚拟环境（不入 Git）
├── .hf_cache/          HuggingFace 模型缓存（不入 Git）
├── chroma_db/          向量数据库文件（不入 Git）
├── data/               原始 PDF：宁德财报×2、货币政策（不入 Git）
├── src/                RAG 核心模块包
│   ├── document_loader.py   文档加载（PDF/Word）
│   ├── text_processor.py    文本分块
│   ├── vector_store.py      向量化 + ChromaDB 存取（HF_HOME 重定向）
│   ├── retriever.py         检索 + 生成（父子文档检索、查询翻译）
│   └── rag_pipeline.py      装配入口（RAGPipeline）
├── rag_app.py          当前后端入口（认证 + RAG + 历史 + 查询改写）
├── auth.py             JWT + 密码哈希工具
├── db.py               SQLite 初始化 + 用户表操作
├── _archive/           历史练习/过渡文件（api.py、main.py、auth_api*.py）
├── users.db            SQLite 数据库文件（不入 Git）
├── .env                真实 API Key（不入 Git）
├── .gitignore          忽略规则（2026-08-03 新增）
├── requirements.txt    依赖清单（2026-08-03 新增）
└── 项目结构.md          本文档
```

## 二、入口与运行方式

| 文件 | 启动命令 | 用途 |
|------|---------|------|
| `rag_app.py` | `uvicorn rag_app:app --port 8000` | 当前唯一后端入口（认证 + RAG + 历史 + 查询改写） |
| `_archive/api.py` | 历史练习 | RAG 问答 API 外壳 |
| `_archive/main.py` | 历史练习 | 命令行：摄入文档 + 提问 |

## 三、规范约定

1. **密钥不落 Git**：`.env` 存真实 Key，已被 `.gitignore` 忽略；别人拿到仓库后复制 `.env.example` 填自己的 Key
2. **数据不入 Git**：`chroma_db/`、`data/`、`users.db`、`.hf_cache/`、`.venv/` 全部忽略
3. **编码**：所有 Python 文件用 UTF-8 without BOM（踩坑 #1）
4. **HF 环境变量时机**：必须在文件最顶部、任何 HF 依赖 import 之前设置（踩坑 #22）
5. **新增依赖**：装新库后同步更新 `requirements.txt`

## 四、当前结构问题（待办，本次不动）

| 问题 | 现状 | 建议 |
|------|------|------|
| 练习文件混入生产 | 已归档 | 2026-08-14 已移至 `_archive/`；不再与正式代码同目录 |
| 双目录分裂 | 真正运行在 `rag_system`；git 仓库 `Enterprise AI Workspace` 里的 RAG 副本已过时（只剩 chroma_db 残留） | 以 `rag_system` 为准；同步/重建 git 副本，不再跨目录复制 `src/` |
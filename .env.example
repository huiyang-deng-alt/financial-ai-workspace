# ============================================
# 金融顾问 AI 工作台 - 环境变量模板
# 复制为 .env 后填入真实值（.env 不会提交到 Git）
# ============================================

# DeepSeek API（RAG 系统使用，变量名 OPENAI_API_KEY 为兼容 openai 库）
# 获取地址：https://platform.deepseek.com/api_keys
OPENAI_API_KEY=sk-your-key-here

# LLM 模型名
DEEPSEEK_MODEL=deepseek-v4-pro

# RAG 系统数据库路径（相对路径即可）
CHROMA_DB_PATH=./chroma_db

JWT_SECRET_KEY=change-me-to-a-random-string-at-least-32-bytes-long

#本机 Windows 和 Docker 容器内分别是不同的值
RERANK_MODEL_PATH=D:/models/bge-reranker-base